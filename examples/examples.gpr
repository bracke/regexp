with "config/regexp_examples_config.gpr";

project Examples is
   for Source_Dirs use ("src", "config");
   for Object_Dir use "obj/" & Regexp_Examples_Config.Build_Profile;
   for Exec_Dir use "bin";
   for Create_Missing_Dirs use "True";

   for Main use
     ("basic_search.adb",
      "case_sensitivity.adb",
      "whole_word.adb",
      "find_from_offsets.adb",
      "find_all_matches.adb",
      "matches_entire.adb",
      "character_classes.adb",
      "preset_patterns.adb",
      "lookaround_options.adb",
      "search_workflow.adb",
      "advanced_search_planning.adb",
      "compile_errors.adb",
      "step_limit.adb");

   package Compiler is
      for Default_Switches ("Ada") use
        Regexp_Examples_Config.Ada_Compiler_Switches & ("-gnat2022", "-gnatyM120", "-gnatX");
   end Compiler;

   --  Bytes out as bytes in. Alire's generated switches carry -gnatW8 and the
   --  binder takes its run-time encoding from the same place, which makes
   --  Ada.Text_IO encode every byte above 127 a second time on the way out:
   --  text that was already UTF-8 -- an accented path, an internationalized
   --  domain name, a translated message -- came out doubled. -Wb is the
   --  compiler default and leaves the bytes alone.
   package Binder is
      for Switches ("Ada") use ("-Es", "-Wb"); --  Symbolic traceback
   end Binder;

end Examples;
